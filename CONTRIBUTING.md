# Contributing to LocalFlow 🌊
1. Create a new component in `src/components/nodes/`.
2. Use the `reactflow` library's `Handle` component.
3. Register your node in `src/App.tsx`.
4. Submit a Pull Request!
